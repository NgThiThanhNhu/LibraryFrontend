export interface BorrowingResponse {
    id: string,
    code: string,
    duration: number,
    dueDate: Date,
    status: string,
    userName: string,
    librarianName: string,
    replyDate: string
}